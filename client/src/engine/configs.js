
/*



		if (alive(prev)){
      if (res > 2 && res < 5){
        return prev +  res * 0.1
      }
      else {
        return prev - res * 0.3
      }
    }
    else{
        if (res > 5){
          return prev
        }
        else {
          return prev + res * 0.1
        }
    }



    типо карты красиво но разростается
     if (alive(prev)){
      if (res > 2 && res < 3 ){
        return res*0.1 + prev
      }
      else {
        return 0.5*prev
      }
    }
    else{
        return (res > 1 && res < 4)
    }

	жизнь непрерывная
	второй соперник  100%
        if (alive(prev)){
      if (res == 2 || res == 3 ){
        return prev + 2
      }
      else {
        return (res - prev < 0)
      }
    }
    else{
        return (res == 3)
    }
    третья расса

     if (alive(prev)){
      if (res == 1 || res == 4 ){
        return prev + 1
      }
      else {
        return (res - prev < 0)
      }
    }
    else{
        if (res == 4){
        return 2
      }
      else {
        return 0
      }
    }
*/